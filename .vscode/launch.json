{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run: generate + php + open browser",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/.vscode/run_server.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": false
    }
  ]
}
